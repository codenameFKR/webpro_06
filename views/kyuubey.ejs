<!Doctype html>
<html>

<head>
    <meta charset="utf-8">
    <title>京葉線の駅</title>
</head>

<body>
    <h2>キュゥべえのセリフ</h2>
    <table>
        <% let number=0 %>
            <% for( let row of data ) { %>
                <tr>
                    <td><a href="/kyuubey/<%= number %>">
                            <%= row.title %>
                        </a></td>
                </tr>
                <% number +=1 %>
                    <% } %>
    </table>
</body>

</html>